void setup() 
{
    pinMode(0, OUTPUT);   //设置LED口为输出模式
    digitalWrite(0, LOW);    // 设置LED 口默认为低电平 
    pinMode(1, OUTPUT);   //设置LED口为输出模式
    digitalWrite(1, LOW);    // 设置LED 口默认为低电平 
    pinMode(2, OUTPUT);   //设置LED口为输出模式
    digitalWrite(2, LOW);    // 设置LED 口默认为低电平 
    pinMode(3, OUTPUT);   //设置LED口为输出模式
    digitalWrite(3, LOW);    // 设置LED 口默认为低电平 
    pinMode(4, OUTPUT);   //设置LED口为输出模式
    digitalWrite(4, LOW);    // 设置LED 口默认为低电平 
    pinMode(5, OUTPUT);   //设置LED口为输出模式
    digitalWrite(5, LOW);    // 设置LED 口默认为低电平 
    pinMode(6, OUTPUT);   //设置LED口为输出模式
    digitalWrite(6, LOW);    // 设置LED 口默认为低电平 
    pinMode(7, OUTPUT);   //设置LED口为输出模式
    digitalWrite(7, LOW);    // 设置LED 口默认为低电平 
    pinMode(8, OUTPUT);   //设置LED口为输出模式
    digitalWrite(8, LOW);    // 设置LED 口默认为低电平 
    pinMode(9, OUTPUT);   //设置LED口为输出模式
    digitalWrite(9, LOW);    // 设置LED 口默认为低电平 
    pinMode(10, OUTPUT);   //设置LED口为输出模式
    digitalWrite(5, LOW);    // 设置LED 口默认为低电平 
    pinMode(11, OUTPUT);   //设置LED口为输出模式
    digitalWrite(11, LOW);    // 设置LED 口默认为低电平 
    pinMode(12, OUTPUT);   //设置LED口为输出模式
    digitalWrite(12, LOW);    // 设置LED 口默认为低电平 
    pinMode(13, OUTPUT);   //设置LED口为输出模式
    digitalWrite(13, LOW);    // 设置LED 口默认为低电平 
    pinMode(14, OUTPUT);   //设置LED口为输出模式
    digitalWrite(14, LOW);    // 设置LED 口默认为低电平 
    
}

void loop() {
  // 
    digitalWrite(1, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(1, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(2, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(2, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(3, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(3, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(4, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(4, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(5, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(5, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(6, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(6, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(7, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(7, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(8, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(8, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(9, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(9, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(10, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(10, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(11, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(11, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(12, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(12, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(13, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(13, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
    digitalWrite(14, HIGH);  // 设置LED 口默认为高电平 
    delay(500); //延时500毫秒
    digitalWrite(14, LOW);  // 设置LED 口为低电平
    delay(500); //延时500毫秒
}